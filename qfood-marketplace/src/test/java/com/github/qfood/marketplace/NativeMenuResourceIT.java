package com.github.qfood.marketplace;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeMenuResourceIT extends MenuResourceTest {

    // Execute the same tests but in native mode.
}